server:
  port: 8080

spring:
  application:
    name: customers
  datasource:
    username: 'amigoscode'
    url: jdbc:postgresql://localhost:5432/jbcode
    password: 'password'
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect  # Corrected dialect name
        format_sql: true
      javax:
        persistence:
          validation:
            mode: none
    hibernate:
      ddl-auto: create-drop

